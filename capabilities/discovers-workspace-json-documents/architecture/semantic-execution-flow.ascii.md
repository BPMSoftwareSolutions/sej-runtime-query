# Discover workspace JSON documents semantic execution

```text
Contract-valid request
        |
        v
Resolve resolves-workspace-discovery-disposition
        |
        v
Immutable resolved authority
        |
        v
Execute declared operations through kernel and ports
        |
        v
Project discovers-workspace-json-documents receipt
```
