# Persist workspace registry snapshot system context

```text
Authorized caller
      |
      v
[ persists-workspace-registry-snapshot ]
      |
      v
External semantic kernel
      |
      v
Declared mechanical ports -> observed testimony -> proof receipt
```
