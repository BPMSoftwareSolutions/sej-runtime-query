# Persist workspace registry snapshot boundary

```text
OWNS
  + atomic-registry-persistence
  + temporary-artifact-authority
  + replacement-verification
  + write-receipt

MUST NOT OWN
  - registry-dto-construction
  - classification-policy
  - output-path-selection
```
