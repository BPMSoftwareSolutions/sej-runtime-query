# Resolve workspace document references semantic execution

```text
Contract-valid request
        |
        v
Resolve resolves-workspace-reference-disposition
        |
        v
Immutable resolved authority
        |
        v
Execute declared operations through kernel and ports
        |
        v
Project resolves-workspace-document-references receipt
```
