# Build workspace registry system context

```text
Authorized caller
      |
      v
[ builds-workspace-registry ]
      |
      v
External semantic kernel
      |
      v
Declared mechanical ports -> observed testimony -> proof receipt
```
