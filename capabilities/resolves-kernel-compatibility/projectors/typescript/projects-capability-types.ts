export const projectsCapabilityTypes = Object.freeze({
  capabilityId: "resolves-kernel-compatibility",
  contextContract: "resolves-kernel-compatibility.input.v1",
  resultContract: "resolves-kernel-compatibility.result.v1"
});
