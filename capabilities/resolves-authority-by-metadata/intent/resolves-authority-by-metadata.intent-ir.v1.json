{
  "intentType": "deterministic-capability-intent.v1",
  "capabilityId": "resolves-authority-by-metadata",
  "title": "Resolve authority by metadata",
  "purpose": "Resolve semantic authority using declared metadata selectors.",
  "actor": "authorized-query-caller",
  "trigger": "a resolves-authority-by-metadata request is submitted",
  "desiredOutcome": "Resolve semantic authority using declared metadata selectors.",
  "constraints": [
    "canonical meaning remains language-neutral",
    "capability-specific decisions remain in semantic authority",
    "the runtime body contains linear semantic delegation only",
    "proof testimony is projected into a canonical receipt",
    "an input matching no declared rule is rejected rather than defaulted"
  ],
  "featureIds": [
    "resolves-authority-by-metadata"
  ],
  "ownedDecision": "resolves-metadata-match-mode",
  "declaredDispositions": [
    "AUTHORITY_RESOLVED_BY_METADATA",
    "AUTHORITY_METADATA_NO_MATCH"
  ]
}
