# Classify workspace JSON document semantic execution

```text
Contract-valid request
        |
        v
Resolve resolves-workspace-document-classification
        |
        v
Immutable resolved authority
        |
        v
Execute declared operations through kernel and ports
        |
        v
Project classifies-workspace-json-document receipt
```
