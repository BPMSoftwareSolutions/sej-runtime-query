export const projectsCapabilityBody = Object.freeze({
  capabilityId: "applies-semantic-projection",
  bodyIdentity: "appliesSemanticProjection",
  invocationSequence: Object.freeze([
    "resolves-projection-authority",
    "executes-resolved-semantic-projection",
    "projects-projection-receipt",
  ]),
});
