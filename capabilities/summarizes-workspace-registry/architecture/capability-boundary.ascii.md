# Summarize workspace registry boundary

```text
OWNS
  + registry-summary-counts
  + summary-field-identity

MUST NOT OWN
  - document-classification
  - candidate-role-resolution
  - filesystem-observation
```
