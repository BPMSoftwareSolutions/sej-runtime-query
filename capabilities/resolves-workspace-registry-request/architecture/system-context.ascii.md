# Resolve workspace registry request system context

```text
Authorized caller
      |
      v
[ resolves-workspace-registry-request ]
      |
      v
External semantic kernel
      |
      v
Declared mechanical ports -> observed testimony -> proof receipt
```
